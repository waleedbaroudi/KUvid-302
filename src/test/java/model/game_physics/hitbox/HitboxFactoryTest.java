package model.game_physics.hitbox;

import org.junit.jupiter.api.Test;
import utils.Coordinates;
import utils.Vector;
import model.game_entities.enums.HitboxType;

import java.util.Arrays;

import static org.junit.jupiter.api.Assertions.*;

class HitboxFactoryTest {

    @Test
    void getHitbox() {
        /**
        Hitbox hitbox = HitboxFactory.getInstance().getHitbox(HitboxType.CircularHitbox, new Vector(new Coordinates(0,0) , new Coordinates(3,3)));

        System.out.println(Arrays.toString(hitbox.getBoundaryCoordinates()));
        **/
    }
}